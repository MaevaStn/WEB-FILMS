<!DOCTYPE html>
<html lang="fr">
	<head>
		<title>MVC with Twig</title>
		<meta http-equiv="content-type" content="text/html; charset=UTF-8"/>
		<link href="https://fonts.googleapis.com/css?family=Ubuntu&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="view/web/css/base.css" media="screen"/>
		<link
		rel="shortcut icon" href="view/web/img/favicon.ico"/>
		<!-- CSS only -->
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
	</head>
	<body>
		<main>
			<form action="" method="POST">
				<div class="container">
					<div class="card border-dark mb-5 mt-5 m-auto" style="width: 30rem">
						<div class="card-body">

							<h1 class="col mt-3 mb-5 m-auto text-center">Inscription</h1>
							<div class="col-8 text-center mb-3 m-auto">
								<input type="text" class="form-control" id="userName" name="userName" placeholder="Nom">
							</div>
							<div class="col-8 text-center mb-3 m-auto">
								<input type="email" class="form-control" id="exampleInputEmail1" name="mailInscr" placeholder="Adresse mail">
							</div>

							<div class="col-8 text-center mb-3 m-auto">
								<input type="password" class="form-control" name="passInscr" placeholder="Mot de passe">
							</div>

							<div class="col-8 text-center mb-3 m-auto">
								<input type="password" class="form-control" name="passConf" placeholder="Saisir à nouveau votre mot de passe">
							</div>
							<div class="col-8 mb-3 git m-auto">
								<button type="submit" class="btn btn-primary" name="form2">Valider</button>
							</div>
						</div>
					</div>
				</div>
				{% if requete is defined %}
					{% if requete == 'added' %}
						<div class="row">
							<div class="col text-center">
								<h5>Inscription prise en compte</h5>
							</div>
						</div>
					{% endif %}
				{% endif %}
				{% if status is defined %}
					{% if status == 'existe' %}
						<div class="row">
							<div class="col text-center">
								<h5>Cet utilisateur est déjà enregistré</h5>
							</div>
						</div>
					{% endif %}
				{% endif %}
				{% if pass is defined %}
					{% if pass == 'invalide' %}
						<div class="row">
							<div class="col text-center">
								<h5>Les mots de passe ne sont pas identique</h5>
							</div>
						</div>
					{% endif %}
				{% endif %}
			</form>
		</main>
	</body>
</html>
